import React from 'react';
import MakeOrder from './MakeOrder'
const MakeBetweenOrder = () => {
  // if (localStorage.getItem(JSON.parse('currentOrder')) === 'undefined') {
  //   localStorage.setItem('currentOrder', 'dupa')
  // }
  // console.log('MakeBetweenOrder', localStorage.getItem(JSON.parse('currentOrder')));

  return (
    <>
      <MakeOrder />
    </>
  );
}

export default MakeBetweenOrder;